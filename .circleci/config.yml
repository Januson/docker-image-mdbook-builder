version: 2.1

orbs:
  docker: circleci/docker@2.1.3

workflows:
  build:
    jobs:
      - build-and-push:
          context:
            - docker-hub-creds
          filters:
            branches:
              only: master

jobs:
  build-and-push:
    docker:
      - image: cimg/base:current
        auth:
          username: $DOCKER_LOGIN
          password: $DOCKER_PASSWORD
    steps:
      - add_ssh_keys:
          fingerprints:
            - "8c:92:8e:ec:f9:ba:bb:7b:f7:91:5f:16:f1:58:64:a2"
      - checkout
      - setup_remote_docker
      - docker/check
      - docker/build:
          image: $DOCKER_LOGIN/$CIRCLE_PROJECT_REPONAME
      - docker/push:
          digest-path: /tmp/digest.txt
          image: $DOCKER_LOGIN/$CIRCLE_PROJECT_REPONAME
      - run:
          command: |
            echo "Digest is: $(</tmp/digest.txt)"
